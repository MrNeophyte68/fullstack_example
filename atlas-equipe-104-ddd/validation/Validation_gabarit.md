# Rapport de validation - Sprint X

Dupliquez une section par fonctionnalité testée. Pour chaque fonctionnalité, indiquez quelle(s) combinaison(s) parmi celles exigées ont été testées et dans quel(s) fichier(s).

## Côté client

### Fonctionnalité : `<nom>`

Combinaison(s) testée(s) et où :

Fichier(s) testé(s) et rôle dans la fonctionnalité :

#### Stratégie de tests
Expliquez comment vous avez décidé de tester cette fonctionnalité : quand les tests couvrent suffisamment le comportement et comment les cas de test ont été choisis.

#### Scénarios principaux pour les tests

Pour chaque scénario, indiquez l'entrée (action de l'utilisateur ou comportement du composant) et le comportement/résultat attendu après le traitement de cette entrée. Dans le cas où le contenu de l'entrée varie le résultat, indiquez les variations de l'entrée et le résultat attendu pour chacune. Ex :
- Quand `<X se produit>`, alors `<résultat attendu>`
- Quand l'utilisateur `<effectue Y>`, alors `<résultat attendu>`

#### Cas limites

Vous devez présenter les cas limites (au moins 2 cas) de manière concise et justifier pourquoi c'est un cas limite et non simplement un scénario normal :
- Cas limite : ... — Pourquoi c'est un vrai cas limite : ...
- Cas limite : ... — Pourquoi c'est un vrai cas limite : ...

## Côté serveur

### Fonctionnalité : `<nom>`

Combinaison(s) testée(s) et où :

Fichier(s) testé(s) et rôle dans la fonctionnalité :

#### Stratégie de tests
Expliquez comment vous avez décidé de tester cette fonctionnalité : quand les tests couvrent suffisamment le comportement et comment les cas de test ont été choisis.

#### Scénarios principaux pour les tests de bout en bout.

Pour chaque scénario, indiquez l'entrée (requête HTTP ou message WS) au serveur et le comportement/résultat attendu après le traitement de cette entrée. Dans le cas où le contenu de l'entrée varie le résultat, indiquez les variations de l'entrée et le résultat attendu pour chacune. Ex :
- Quand le système reçoit `<message>`, alors `<comportement/résultat attendu>`
- Quand le système reçoit `<message>`, alors `<comportement/résultat attendu>`

#### Cas limites

Vous devez présenter les cas limites (au moins 2 cas) de manière concise et justifier pourquoi c'est un cas limite et non simplement un scénario normal :
- Cas limite : ... — Pourquoi c'est un vrai cas limite : ...
- Cas limite : ... — Pourquoi c'est un vrai cas limite : ...
